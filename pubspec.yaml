name: elliptical_progress_bar
description: A flutter package to create elliptical progress bar.
version: 0.0.1
homepage: https://github.com/A7ALABS/elliptical-progress-bar

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:


  
