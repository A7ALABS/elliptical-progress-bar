name: elliptical_progress_bar
description: A pretty flutter package to create elliptical progress bars with multiple customisation options, such as the ability to set the colors, stroke width, text style and more.
version: 0.0.1+7
homepage: https://github.com/A7ALABS/elliptical-progress-bar
issue_tracker: https://github.com/A7ALABS/elliptical-progress-bar/issues

platforms:
  android:
  ios:
  web:
  macos:
  windows:
  linux:

topics:
  - progress-bar
  - progress-indicator
  - elliptical-progress-bar
  - ellipse

screenshots:
  - description: 'This screenshot shows the progress bar with different customisations (Android).'
    path: example/screenshots/android.png
  - description: 'This screenshot shows the progress bar with different customisations (iOS).'
    path: example/screenshots/ios.png

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: